import 'package:flutter/material.dart';

class Course extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Center(
      child:Image.asset('images/class_bg.png')
    );
  }

}